services:
  - type: web
    name: vibeshop-portal
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: "false"
      - key: DATABASE_URL
        fromDatabase:
          name: vibeshop-db
          property: connectionString
      - key: GOOGLE_PROJECT_ID
        value: ""
      - key: GOOGLE_REGION
        value: us-central1

databases:
  - name: vibeshop-db
    engine: postgres
    version: "15"
    plan: free
    initialDatabase: vibeshop_db
